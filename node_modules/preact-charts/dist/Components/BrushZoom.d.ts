import { Component, VNode } from 'preact';
import { NumberTuple, Margin } from '../types';
interface BrushProps {
    height: number;
    width: number;
    margin: Margin;
    onBrush?: (extent: [NumberTuple, NumberTuple] | null) => void;
}
interface BrushState {
    extent: [NumberTuple, NumberTuple] | null;
    mouseDown: boolean;
}
export declare class BrushZoom extends Component<BrushProps, BrushState> {
    static defaultProps: {
        onBrush: () => void;
    };
    constructor(props: BrushProps);
    render({ height, width }: BrushProps, { extent, mouseDown }: BrushState): VNode;
    private handleMouseDown;
    private handleMouseMove;
    private handleMouseUp;
    private handleDblClick;
}
export {};
