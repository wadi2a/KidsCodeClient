import { Component, VNode } from 'preact';
import { ChartProps, ChartDefaultProps } from '../types';
interface DiffBarProps extends ChartProps {
    data: {
        name: string;
        value: number;
    }[];
    ticks?: number;
}
interface DiffBarDefaultProps extends ChartDefaultProps {
    ticks: number;
}
interface DiffBarState {
    width: number;
    innerWidth: number;
    height: number;
    innerHeight: number;
}
export declare class DifferenceChart extends Component<DiffBarProps, DiffBarState> {
    static defaultProps: DiffBarDefaultProps;
    private chartSVG;
    private resizeOb;
    constructor(props: DiffBarProps);
    render({ margin, ticks, data, name }: DiffBarProps, { height, width, innerHeight, innerWidth }: DiffBarState): VNode;
    componentDidMount(): void;
    componentWillUnmount(): void;
    private resizeChart;
    private createBars;
}
export {};
