import { Component, VNode } from 'preact';
import { TimestampArray, ChartProps, ChartDefaultProps } from '../types';
interface RangeChartProps extends ChartProps {
    y: string;
    data: TimestampArray;
    lineColour?: string;
    fillColour?: string;
    onBrush?: (extent: Date[]) => void;
    brushColour?: string;
}
interface RangeChartDefaultProps extends ChartDefaultProps {
    lineColour?: string;
    fillColour?: string;
    onBrush?: (extent: Date[]) => void;
    brushColour: string;
}
interface RangeChartState {
    width: number;
    innerWidth: number;
    height: number;
    innerHeight: number;
    extent?: number[];
}
export declare class RangeChart extends Component<RangeChartProps, RangeChartState> {
    static defaultProps: RangeChartDefaultProps;
    private xScale;
    private chartSVG;
    private resizeOb;
    constructor(props: RangeChartProps);
    render(props: RangeChartProps, { width, height, innerWidth, innerHeight }: RangeChartState): VNode;
    componentDidMount: () => void;
    componentWillUnmount(): void;
    private resizeChart;
    private handleBrush;
}
export {};
