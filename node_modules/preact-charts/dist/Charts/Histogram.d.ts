import { Component, VNode } from 'preact';
import { DataArray, ChartProps, ChartDefaultProps } from '../types';
interface HistogramProps extends ChartProps {
    x: string;
    data: DataArray;
    ticks?: number;
    barColour?: string;
    barOutline?: string;
}
interface HistogramDefaultProps extends ChartDefaultProps {
    ticks: number;
    barColour: string;
    barOutline: string;
}
interface HistogramState {
    width: number;
    innerWidth: number;
    height: number;
    innerHeight: number;
}
export declare class Histogram extends Component<HistogramProps, HistogramState> {
    static defaultProps: HistogramDefaultProps;
    private chartSVG;
    private resizeOb;
    constructor(props: HistogramProps);
    render({ name, margin, x, data, ticks, barColour, barOutline }: HistogramProps, { height, width, innerHeight, innerWidth }: HistogramState): VNode;
    componentDidMount(): void;
    componentWillUnmount(): void;
    private resizeChart;
}
export {};
