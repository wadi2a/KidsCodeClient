import { Component, VNode } from 'preact';
import { GroupedDataObject, ChartProps, ChartDefaultProps } from '../types';
interface GroupedBarProps extends ChartProps {
    data: GroupedDataObject;
    groups: string[];
    legendReference: {
        [key: string]: string;
    };
    ticks?: number;
}
interface GroupedBarDefaultProps extends ChartDefaultProps {
    ticks?: number;
}
interface GroupedBarState {
    width: number;
    innerWidth: number;
    height: number;
    innerHeight: number;
}
export declare class GroupedBar extends Component<GroupedBarProps, GroupedBarState> {
    static defaultProps: GroupedBarDefaultProps;
    private chartSVG;
    private resizeOb;
    constructor(props: GroupedBarProps);
    render({ margin, ticks, data, groups, legendReference, name }: GroupedBarProps, { height, width, innerHeight, innerWidth }: GroupedBarState): VNode;
    componentDidMount(): void;
    componentWillUnmount(): void;
    private resizeChart;
    private createBars;
}
export {};
