import { Component, VNode } from 'preact';
import { DataArray, ChartProps, ChartDefaultProps, NumberTuple } from '../types';
interface ScatterPlotProps extends ChartProps {
    x: string;
    y: string;
    data: DataArray;
    radius?: number;
    labels?: boolean;
    dotFill?: string;
    dotBorder?: string;
    regressionEq?: (x: number) => number;
    regLineColor?: string;
}
interface ScatterPlotDefaultProps extends ChartDefaultProps {
    radius: number;
    labels: boolean;
    dotFill: string;
    dotBorder: string;
    regLineColor: string;
}
interface ScatterPlotState {
    width: number;
    innerWidth: number;
    height: number;
    innerHeight: number;
    xDomain: NumberTuple;
    yDomain: NumberTuple;
}
export declare class ScatterPlot extends Component<ScatterPlotProps, ScatterPlotState> {
    static defaultProps: ScatterPlotDefaultProps;
    private chartSVG;
    private resizeOb;
    private xScale;
    private yScale;
    constructor(props: ScatterPlotProps);
    render(props: ScatterPlotProps, { height, width, innerHeight, innerWidth, xDomain, yDomain }: ScatterPlotState): VNode;
    componentDidMount(): void;
    componentWillReceiveProps(newProps: ScatterPlotProps): void;
    componentWillUnmount(): void;
    private resizeChart;
    private handleBrush;
}
export {};
