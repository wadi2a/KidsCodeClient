import { Component, VNode } from 'preact';
import { GroupedDataObject, ChartProps, ChartDefaultProps } from '../types';
interface HorizontalBarProps extends ChartProps {
    data: GroupedDataObject;
    groups: string[];
    legendReference: {
        [key: string]: string;
    };
    ticks?: number;
}
interface HorizontalBarDefaultProps extends ChartDefaultProps {
    ticks?: number;
}
interface HorizontalBarState {
    width: number;
    innerWidth: number;
    height: number;
    innerHeight: number;
}
export declare class HorizontalBar extends Component<HorizontalBarProps, HorizontalBarState> {
    static defaultProps: HorizontalBarDefaultProps;
    private chartSVG;
    private resizeOb;
    constructor(props: HorizontalBarProps);
    render({ margin, ticks, data, groups, name, legendReference }: HorizontalBarProps, { height, width, innerHeight, innerWidth }: HorizontalBarState): VNode;
    componentDidMount(): void;
    componentWillUnmount(): void;
    private resizeChart;
    private createBars;
}
export {};
