import { Component, VNode } from 'preact';
import { DataArray, ChartProps, ChartDefaultProps } from '../types';
interface LineScatterProps extends ChartProps {
    x: string;
    y: string;
    data: DataArray[];
    radius?: number;
    labels?: boolean;
    legendReference?: string[];
}
interface LineScatterDefaultProps extends ChartDefaultProps {
    radius: number;
    labels: boolean;
}
interface LineScatterState {
    width: number;
    innerWidth: number;
    height: number;
    innerHeight: number;
    xDomain: [number, number];
    yDomain: [number, number];
}
export declare class LineScatter extends Component<LineScatterProps, LineScatterState> {
    static defaultProps: LineScatterDefaultProps;
    private chartSVG;
    private resizeOb;
    private xScale;
    private yScale;
    constructor(props: LineScatterProps);
    render(props: LineScatterProps, { height, width, innerHeight, innerWidth, xDomain, yDomain }: LineScatterState): VNode;
    componentDidMount(): void;
    componentWillReceiveProps(newProps: LineScatterProps): void;
    componentWillUnmount(): void;
    private resizeChart;
    private handleBrush;
}
export {};
