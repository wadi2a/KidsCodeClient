import { DataArray, DataObject } from '../types';
import { InterpolatorFunction } from './interpolators';
export declare type ValueAccessor = (point: DataObject) => number;
export interface LineArgument {
    x: ValueAccessor;
    y: ValueAccessor;
    interpolation?: InterpolatorFunction;
}
export declare function line({ x, y, interpolation }: LineArgument): (data: DataArray) => string;
