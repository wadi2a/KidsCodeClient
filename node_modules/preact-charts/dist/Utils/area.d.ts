import { DataArray } from '../types';
import { LineArgument } from './line';
interface AreaArgument extends LineArgument {
    y0: number;
}
export declare function area({ x, y, y0, interpolation }: AreaArgument): (data: DataArray) => string;
export {};
