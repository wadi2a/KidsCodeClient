import { linearInterpolation } from './interpolators';
export function area({ x, y, y0, interpolation }) {
    return function (data) {
        return data.reduce((acc, point, i, a) => i === a.length - 1 ?
            `${acc} ${interpolation ? interpolation(point, x, y, i, a) : linearInterpolation(point, x, y)} V${y0} Z` :
            `${acc} ${interpolation ? interpolation(point, x, y, i, a) : linearInterpolation(point, x, y)}`, `M0,${y0}`);
    };
}
//# sourceMappingURL=area.js.map