const initialState = {
    position : [0,0],
}

const voitureReducer = (state = initialState,action)=>{
    switch (action.type) {
        default :
            return state

    }
}

export  default voitureReducer